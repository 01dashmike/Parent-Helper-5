// Note: This config is for editor or tooling only.
// It is not used by the Next.js build process.
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": [
    "src"
  ]
}
